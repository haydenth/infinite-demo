version: '3.8'

services:
  offramp-service:
    build: ./offramp-service
    container_name: offramp-service
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    networks:
      - infinite-network

networks:
  infinite-network:
    driver: bridge
